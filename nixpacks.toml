# Nixpacks 配置文件 - Railway 自动检测并使用

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install", "cd server && npm install", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "NODE_ENV=production node server/index.js"
